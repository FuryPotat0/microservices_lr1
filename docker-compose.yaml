version: "3.9"
services:
  postgres:
    image: postgres:15.3-alpine
    environment:
      POSTGRES_DB: "microservices"
      POSTGRES_USER: "pguser"
      POSTGRES_PASSWORD: "pguserpass"
    volumes:
      - .:/docker-entrypoint-initdb/data
    ports:
      - "5433:5432"

